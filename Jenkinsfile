pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'echo "build"'
      }
    }
    stage('code upload') {
      steps {
        sh 'echo code upload'
      }
    }
    stage('activate code version') {
      steps {
        sh 'echo activate code version'
      }
    }
  }
}